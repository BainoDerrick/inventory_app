import 'package:flutter/material.dart';

class InventoryListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Inventory List'),
      ),
      body: Center(
        child: Text('Inventory List Here'),
      ),
    );
  }
}
